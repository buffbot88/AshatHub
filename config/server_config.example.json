{
  "// 0": "═══════════════════════════════════════════════════════════════",
  "// 1": "ASHAT Hub — example server configuration",
  "// 2": "Copy this file to config/server_config.json and fill in YOUR values.",
  "// 3": "server_config.json itself is gitignored and must never be committed.",
  "// 4": "Lines whose key starts with '//' are documentation and are ignored",
  "// 5": "by the loader (config/bootstrap.php).",
  "// 6": "═══════════════════════════════════════════════════════════════",
  "// 7": "APP_* — application settings",
  "APP_NAME": "ASHAT Hub",
  "APP_ENV": "development",
  "APP_DEBUG": true,
  "APP_URL": "http://localhost:8000",
  "APP_TIMEZONE": "UTC",
  "// 8": "DB_* — MySQL/MariaDB connection (required for the site to run)",
  "DB_HOST": "localhost",
  "DB_PORT": 3306,
  "DB_NAME": "ashat_hub",
  "DB_USER": "root",
  "DB_PASS": "",
  "// 9": "SESSION_* — session tuning (optional, defaults shown)",
  "SESSION_LIFETIME": 7200,
  "SESSION_COOKIE_NAME": "ashat_sid",
  "SESSION_SECURE_COOKIE": false,
  "// 10": "BRAINSTEM_* — server-side inference backend (admin settings can",
  "// 11": "override these; blank key falls back to the local default)",
  "BRAINSTEM_URL": "http://localhost:7860",
  "BRAINSTEM_KEY": "",
  "// 12": "DEBUG_TOKEN — optional; enables ?debug=1&t=TOKEN for in-browser",
  "// 13": "stack traces on 500s. Leave blank to disable.",
  "DEBUG_TOKEN": "",
  "// 14": "APP_KEY — reserved for future signing/encryption, not read yet",
  "APP_KEY": "",
  "// 15": "PAWS_SHARED_SECRET — server-to-server trust secret used by Paws &",
  "// 16": "Parcels' game server to verify an Ashat Hub session ID at",
  "// 17": "POST /api/sso/verify-session. Generate cryptographically random,",
  "// 18": "≥ 32 chars, and copy the SAME value into the Paws server's",
  "// 19": "server_config.json under auth.ashatHub.sharedSecret.",
  "PAWS_SHARED_SECRET": "REPLACE_ME_ASHAT_TO_PAWS_SHARED_SECRET_32_CHARS_MIN"
}
