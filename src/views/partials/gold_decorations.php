<?php
/**
 * Legacy decorative effects (floating particles, spinning gears).
 *
 * Removed in the "Plainspoken" redesign — the class names still exist
 * in app.css but are inert. This partial intentionally outputs nothing;
 * it is kept so existing `include` calls keep working.
 */
