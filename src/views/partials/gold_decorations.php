<?php
/**
 * Legacy decorative effects (floating particles, spinning gears), removed
 * in the "Plainspoken" redesign — the class names still exist in app.css
 * but are inert. This partial intentionally outputs nothing; kept so
 * existing `include` calls keep working.
 */
